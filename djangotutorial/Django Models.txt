Inserting a record:

python3 manage.py shell

    from usermodel.models import User
    u = User(name='nombre', email='email@invent')
    u.save()
    print(u.id)
    print(u.email)

Equivalente a INSERT INTO tabla (campos) VALUES (valor1, valor2)

Ejemplo de FOREIGN KEY:

Class x(models.Model):
    variable = models.ForeignKey('VariableExterna', on_delete=models.SET_NULL, null=True)
    Si se borra el anfitrión de la  variable externa, set null (dejarlo vacío)

    variable2 = models.ForeignKey('variabelexterna', on_delete=models.CASCADE) -> Eliminar en cascada
